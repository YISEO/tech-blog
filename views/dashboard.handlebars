{{#if posts.length}}
    <h2>Your Posts</h2>
    {{#each posts as |post|}}
    {{> dashboard-post}}
    {{/each}}
{{/if}}

<div class="button-wrap">
    <a class="link" href="/dashboard/new">+ New Post</a>
</div>
