{{#if posts.length}}
<h2>Your Posts</h2>
{{#each posts as |post|}}
{{> dashboard-post}}
{{/each}}

<button type="button" class="btn">
    <a class="link" href="/dashboard/new">+ New Post</a>
</button>
{{/if}}

